(ns sicp.chapter02.2-72)

; Consider the maximum and minimum
; The minimum value is the case of a full binary tree O(n * log_n)
; The maximum value is like in 2.71 is O(n^2)
; The final answer os between O(n * log_n) and O(n^2)
